---
import { calculateYears, numberToWords, birthDate, itStartDate, webDevStartDate } from '../utils/date';

interface Props {
    lang: 'en' | 'es';
}

const { lang } = Astro.props;

const age = calculateYears(birthDate);
const itYears = calculateYears(itStartDate);
const webDevYears = calculateYears(webDevStartDate);

const ageText = numberToWords(age, lang);
const itYearsText = numberToWords(itYears, lang);
const webDevYearsText = numberToWords(webDevYears, lang);
---

{lang === 'en' ? (
    <p>
        Hi 👋<br/><br/>
        My name is Alberto and I'm a Web Developer!<br/><br/>
        I'm {ageText} years old and I'm more than {itYearsText} years in the IT world of which {webDevYearsText} of them I've been completely focused in the most I like, Web Development.<br/><br/>
        Among other things, I like it because it implies a constant evolution in terms of knowledge and skills. It seems to me that it is what enriches us as professionals and people.<br/><br/>
        As a Web Developer I know that the languages and technologies are only the tool and way and not the finish, that's because we should be able to find a solution regardless of them. But although I know this I can't help it feel a special love for JavaScript and the whole new ecosystem that it's growing around him(Node, React, Vue, Angular, etc)<br/><br/>
        But I don't only enjoy my time being on the computer, I'm a big fan of the sport and I really like to expend time with my friends and family.
    </p>
) : (
    <p>
        Hola 👋<br/><br/>
        Me llamo Alberto y soy Desarrollador Web!<br/><br/>
        Tengo {ageText} años y llevo más de {itYearsText} en el mundo de la informática de los cuales {webDevYearsText} he estado totalmente enfocado en lo que más me gusta, el desarrollo web.<br/><br/>
        Entre otras cosas me gusta porque implica una constante evolución a nivel de conocimientos y habilidades. Me parece que es lo que nos enriquece como profesionales y personas.<br/><br/>
        Como desarrollador web soy consciente de que los lenguajes y tecnologías son solo el medio y no el fin por lo que debemos ser capaces de hallar una solución independientemente de estos. Pero a pesar de esto no puedo evitar sentir una gran debilidad por JavaScript y todo el ecosistema que ha ido creciendo a su alrededor(Node, React, Vue, Angular, etc)<br/><br/>
        Pero no solo me gusta estar pegado al ordenador, soy un gran fan del deporte y me encanta pasar tiempo con los mios.
    </p>
)}
