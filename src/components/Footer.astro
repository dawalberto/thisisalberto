---
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const year = new Date().getFullYear();
const madeWithText = lang === 'en' ? 'Made with 💛 by' : 'Hecho con 💛 por';
---
<footer class="py-6 sm:py-8 border-t-2 border-slate-900 bg-slate-900">
    <div class="max-w-4xl mx-auto px-3 sm:px-4 text-center text-slate-300 text-xs sm:text-sm font-medium">
        <p>
            {madeWithText}
            <span class="relative inline-block">
                <span class="relative z-10 text-white font-semibold">Alberto García Sola</span>
                <span class="absolute bottom-0 left-0 w-full h-0.5 sm:h-1 bg-accent -rotate-2"></span>
            </span>
        </p>
        <p class="mt-1.5 sm:mt-2 font-bold text-white">&copy; 2021 / {year}</p>
    </div>
</footer>
